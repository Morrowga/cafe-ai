# CaféAI — Backend Dependencies

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation — let pip resolve the right version
pydantic>=2.7.4,<3.0.0

# Environment variables
python-dotenv>=1.0.0

# LangChain + OpenAI — let pip resolve compatible versions
langchain>=1.0.0  
langchain-openai>=0.2.0
# langchain-anthropic>=0.1.0  ← uncomment when switching to Claude

# Hugging Face (emotion detection)
transformers>=4.40.0
torch>=2.0.0         

# HTTP
httpx>=0.27.0